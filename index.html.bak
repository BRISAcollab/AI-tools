<!doctype html><html lang="pt-BR"><head>  <meta charset="utf-8" />  <meta name="viewport" content="width=device-width, initial-scale=1" />  <title>Triagem PICO — Front-end</title>  <link rel="stylesheet" href="style.css" />  <!-- SheetJS para ler .xlsx/.xls no navegador -->  <script src="https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js" defer></script>  <script src="app.js" defer></script></head><body>  <header class="appbar">    <h1>Triagem de Títulos e Resumos (PICO)</h1>    <div class="version">Front-end MVP</div>  </header>  <main class="container">    <section class="card">      <h2>Configuração do modelo</h2>      <div class="grid">        <label class="field">          <span>Modelo OpenAI</span>          <select id="modelSelect" required>            <option value="" disabled selected>Selecione o modelo…</option>            <option value="gpt-4o">GPT-4o</option>            <option value="gpt-4o-mini">GPT-4o-mini</option>            <option value="gpt-5">GPT-5</option>            <option value="gpt-5-mini">GPT-5-mini</option>            <option value="o3">O3 (razão)</option>          </select>          <small>Dica: para custo menor em rascunhos, use a versão mini.</small>        </label>        <label class="field">          <span>Endpoint do back-end (opcional)</span>          <input id="apiBase" type="url" placeholder="http://localhost:8000" />          <small>Deixe em branco se quiser só gerar o payload JSON.</small>        </label>      </div>    </section>    <section class="card">      <h2>Contexto do estudo</h2>      <label class="field">        <span>Síntese do estudo (1–3 frases)</span>        <textarea id="studySynopsis" rows="3" placeholder="Ex.: Estamos revisando os efeitos da minociclina em comportamento depressivo em roedores..."></textarea>      </label>      <div class="grid">        <label class="field">          <span>Critérios de inclusão</span>          <textarea id="inclusionCriteria" rows="6" placeholder="• desenho: ensaios controlados com braços separados&#10;• população: ratos ou camundongos..."></textarea>          <small>Um por linha. Use marcadores se desejar.</small>        </label>        <label class="field">          <span>Critérios de exclusão</span>          <textarea id="exclusionCriteria" rows="6" placeholder="• relatos de caso, editoriais, revisões&#10;• espécies diferentes de ratos/camundongos..."></textarea>          <small>Um por linha. Use marcadores se desejar.</small>        </label>      </div>    </section>    <section class="card">      <h2>Planilha</h2>      <div class="uploader" id="dropzone">        <input id="fileInput" type="file" accept=".xlsx,.xls,.csv" />        <p class="muted">Arraste e solte aqui ou clique para selecionar .xlsx/.xls/.csv</p>        <p id="fileMeta" class="file-meta"></p>      </div>      <div id="sheetPickerWrap" class="sheet-picker hidden">        <label class="field">          <span>Aba da planilha</span>          <select id="sheetSelect"></select>        </label>      </div>      <div id="columnsStatus" class="status muted">Esperando arquivo…</div>      <div id="previewWrap" class="table-wrap hidden">        <table id="previewTable">          <thead></thead>          <tbody></tbody>        </table>        <small>Pré-visualização das 5 primeiras linhas.</small>      </div>    </section>    <section class="card actions">      <button id="btnGenerate" class="primary">Gerar payload JSON</button>      <button id="btnSend" class="secondary" disabled>Enviar ao back-end</button>      <span id="saveHint" class="muted">As configurações salvam automaticamente.</span>    </section>    <section id="payloadCard" class="card hidden">      <h2>Payload gerado</h2>      <pre id="payloadOut" class="code"></pre>      <button id="btnCopy" class="ghost">Copiar</button>    </section>  </main>  <footer class="footer">    <span>Feito para triagem rápida e transparente.</span>  </footer></body></html>